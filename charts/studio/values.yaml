# Default values for the course-intelligence-studio chart.

nameOverride: ""
fullnameOverride: ""

replicaCount: 1

image:
  repository: ghcr.io/bcit-tlu/course-intelligence/course-intelligence-studio
  tag: ""
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80

# Backend service discovery. The chart builds a cluster-internal FQDN
# (<host>.<namespace>.svc.cluster.local:<port>) unless env.BACKEND_URL
# overrides it entirely.
backend:
  host: course-intelligence-backend
  port: 8000
  namespace: ""

env:
  # Explicit override for the nginx upstream. Leave empty to auto-build
  # the backend Service FQDN (see course-intelligence-studio.backendUrl helper).
  BACKEND_URL: ""

# OTel collector endpoint for frontend trace export. The nginx config
# proxies /otel/* to this URL. Leave empty to disable frontend analytics.
otel:
  endpoint: ""

ingress:
  enabled: false
  className: ""
  annotations:
    # Allow large course-module (D2L zip) uploads through the ingress.
    nginx.ingress.kubernetes.io/proxy-body-size: "0"
    nginx.ingress.kubernetes.io/proxy-request-buffering: "off"
  hosts: []
  tls: []

resources: {}

nodeSelector: {}
tolerations: []
affinity: {}
