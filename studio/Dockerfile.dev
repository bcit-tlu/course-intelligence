FROM node:20-alpine

WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci

COPY . .

EXPOSE 5173

CMD ["sh", "-c", "echo BACKEND_URL=$BACKEND_URL > /app/.env.local && npm run dev -- --host 0.0.0.0"]
