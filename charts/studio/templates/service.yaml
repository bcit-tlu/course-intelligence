apiVersion: v1
kind: Service
metadata:
  name: {{ include "course-intelligence-studio.fullname" . }}
  labels:
    {{- include "course-intelligence-studio.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "course-intelligence-studio.selectorLabels" . | nindent 4 }}
